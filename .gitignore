/target
/interview/
CLAUDE.md
.DS_Store

# Fault-harness run artifacts: keep summaries, CSVs, logs, and plots in the
# repo; the raw per-run history files are large and re-generated by a run.
bench/results/faults/**/history.txt
target/faults/
